<?xml version="1.0" encoding="utf-8"?>
<odoo>

<!-- Registration Action -->
    <record id="training_action_registration" model="ir.actions.act_window">
      <field name="name">Registration</field>
      <field name="res_model">training.registration</field>
      <field name="view_mode">list,form</field>
    </record>   
<!-- Registration Action -->

<!-- Registration List -->
   <record model="ir.ui.view" id="training_registration_list">
      <field name="name">Registration List</field>
      <field name="model">training.registration</field>
      <field name="arch" type="xml">
        <list>
          <field name="name"/>
        </list>
      </field>
    </record>
<!-- Registration List -->

<!-- Registration Form -->
   <!-- <record model="ir.ui.view" id="training_registration_form_hr">
      <field name="name">Registration Form</field>
      <field name="model">training.registration</field>
      <field name="groups_id" eval="[(4, ref('training.group_training_hr'))]"/>
      <field name="arch" type="xml">
        <form>
          <group>
            <field name="name"/>
            <field name="course_id" domain="[('deadline', '&gt;=', context_today())]"/>
            <field name="course_description"/>
            <field name="teacher_id"/>
            <field name="start_date"/>
            <field name="end_date"/>
            <field name="number_of_days"/>
            <field name="start_time" />
            <field name="end_time" />
            <field name="room_ids" />
            <field name="location_ids" />
            <field name="trainee_ids" domain="[('id', '!=',teacher_id)]"/>
          </group>
        </form>
      </field>
    </record> -->
<!-- Registration Form -->

<!-- Requester Registration Form -->
    <record model="ir.ui.view" id="training_registration_form_requester">
      <field name="name">Registration Form</field>
      <field name="model">training.registration</field>
      <field name="groups_id" eval="[(4, ref('training.group_training_requester'))]"/>
      <field name="arch" type="xml">
        <form>
          <group>
            <field name="name"/>
            <field name="course_id" domain="[('deadline', '&gt;=', context_today())]"/>
            <field name="teacher_id"/>
          </group>
        </form>
      </field>
    </record>
<!-- Requester Registration Form -->
</odoo>
